name: Deploy to Hetzner

on:
  push:
    branches: [redesign/radical-overhaul]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 94.130.111.231
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            nvm use 20
            cd /var/www/attorney-site
            git fetch origin
            git checkout redesign/radical-overhaul
            git pull origin redesign/radical-overhaul
            cd frontend
            npm install
            npm run build
            cd ..
            pm2 restart lawyer-nuxt
            pm2 restart lawyer-api